// アプリケーション
import { actions, mutations } from './mutations';
import { getters } from './getters';

export default {
  namespaced: true,
  actions: actions,
  mutations: mutations,
  getters: getters,
  state: {
  }
}
